import React from "react";
import { ChevronRight, PhoneCall, Shield, Zap, Wrench } from "lucide-react";
import { useLanguage } from "../context/LanguageContext";

const Hero = () => {
  const { strings } = useLanguage();

  return (
    <section id="home" className="hero-section">
      {/* ── Video background ── */}
      <video
        className="hero-video"
        poster="https://bscpowertools.com/wp-content/uploads/2024/04/Chainsaw-2-2-scaled.jpg"
        autoPlay
        loop
        muted
        playsInline
      >
        <source
          src="https://bscpowertools.com/wp-content/uploads/2024/02/BSC-7200-Chainsaw.mp4#t=5"
          type="video/mp4"
        />
      </video>

      {/* ── Very light vignette so text stays readable ── */}
      <div className="hero-overlay" />

      {/* ── Content anchored at bottom-left ── */}
      <div className="hero-content">
        {/* Badge */}
        <div className="hero-badge">
          <span className="hero-badge-dot" />
          {strings.topRated || "Top Rated Industrial Supplier in MP"}
        </div>

        {/* Headline */}
        <h1 className="hero-h1">
          {strings.heroTitle || "VPT Tools Touch Sidhi"}
          <br />
          <span className="hero-h1-accent">{strings.heroAccent || "Dushyant_Power_Tools"}</span>
        </h1>

        {/* Description */}
        <p className="hero-desc">
          {strings.heroDescription || "Heavy-duty tools, premium spare parts & diamond blades — engineered for professionals."}
        </p>

        {/* CTA row */}
        <div className="hero-cta-row">
          <a href="#products" className="hero-btn-primary">
            {strings.exploreCatalog || "Explore Catalog"} <ChevronRight size={18} />
          </a>
          <a href="tel:+919754015503" className="hero-btn-outline">
            <PhoneCall size={16} /> {strings.contactSales || "Contact Sales"}
          </a>
        </div>

        {/* Trust badges */}
        <div className="hero-trust">
          <div className="hero-trust-item">
            <Shield size={16} className="hero-trust-icon" />
            <span>{strings.certifiedParts || "Certified Parts"}</span>
          </div>
          <div className="hero-trust-divider" />
          <div className="hero-trust-item">
            <Zap size={16} className="hero-trust-icon" />
            <span>{strings.heavyDuty || "Heavy Duty"}</span>
          </div>
          <div className="hero-trust-divider" />
          <div className="hero-trust-item">
            <Wrench size={16} className="hero-trust-icon" />
            <span>{strings.expertRepair || "Expert Repair"}</span>
          </div>
        </div>
      </div>

      {/* ── Stats strip — right side ── */}
      <div className="hero-stats">
        {[
          { n: "1000+", l: "Tools" },
          { n: "500+", l: "Customers" },
          { n: "10+", l: "Years" },
          { n: "100+", l: "Brands" },
        ].map(({ n, l }) => (
          <div key={l} className="hero-stat-card">
            <div className="hero-stat-num">{n}</div>
            <div className="hero-stat-lbl">{l}</div>
          </div>
        ))}
      </div>

      <style>{`
        /* ═══════════════════════════════════════════
           HERO SECTION STYLES
        ═══════════════════════════════════════════ */
        .hero-section {
          position: relative;
          width: 100%;
          min-height: 100vh;
          overflow: hidden;
          display: flex;
          align-items: flex-end;
          font-family: 'Inter', sans-serif;
        }

        /* Full-screen video */
        .hero-video {
          position: absolute;
          inset: 0;
          width: 100%;
          height: 100%;
          object-fit: cover;
          z-index: 0;
        }

        /* *** KEY CHANGE — very light overlay so video shines *** */
        .hero-overlay {
          position: absolute;
          inset: 0;
          z-index: 1;
          /* transparent at top, slight dark at bottom for text readability */
          background: linear-gradient(
            to bottom,
            rgba(0, 0, 0, 0.08) 0%,
            rgba(0, 0, 0, 0.05) 50%,
            rgba(0, 0, 0, 0.62) 85%,
            rgba(0, 0, 0, 0.80) 100%
          );
        }

        /* Content panel (bottom-left) */
        .hero-content {
          position: relative;
          z-index: 2;
          padding: 0 40px 56px;
          max-width: 580px;
        }

        /* Badge */
        .hero-badge {
          display: inline-flex;
          align-items: center;
          gap: 8px;
          background: rgba(255,255,255,0.12);
          border: 1px solid rgba(255,255,255,0.25);
          backdrop-filter: blur(10px);
          color: rgba(255,255,255,0.93);
          font-size: 10px;
          font-weight: 700;
          letter-spacing: 0.15em;
          text-transform: uppercase;
          padding: 6px 14px;
          border-radius: 0;
          margin-bottom: 20px;
        }
        .hero-badge-dot {
          display: inline-block;
          width: 8px;
          height: 8px;
          border-radius: 0;
          background: #dc2626;
          box-shadow: 0 0 0 0 rgba(220,38,38,0.5);
          animation: heroPulse 1.4s infinite;
          flex-shrink: 0;
        }
        @keyframes heroPulse {
          0%   { box-shadow: 0 0 0 0   rgba(220,38,38,0.6); }
          70%  { box-shadow: 0 0 0 8px rgba(220,38,38,0); }
          100% { box-shadow: 0 0 0 0   rgba(220,38,38,0); }
        }

        /* Headline */
        .hero-h1 {
          font-size: clamp(36px, 5.5vw, 72px);
          font-weight: 900;
          line-height: 1.07;
          color: #fff;
          margin: 0 0 16px;
          letter-spacing: -0.03em;
          text-shadow: 0 2px 12px rgba(0,0,0,0.3);
        }
        .hero-h1-accent {
          background: linear-gradient(90deg, #f87171, #fb923c);
          -webkit-background-clip: text;
          -webkit-text-fill-color: transparent;
          background-clip: text;
          filter: drop-shadow(0 2px 10px rgba(248,113,113,0.45));
        }

        /* Description */
        .hero-desc {
          font-size: 14px;
          color: rgba(255,255,255,0.78);
          line-height: 1.7;
          margin: 0 0 24px;
          font-weight: 400;
          max-width: 440px;
        }

        /* CTA buttons */
        .hero-cta-row {
          display: flex;
          flex-wrap: wrap;
          gap: 12px;
          margin-bottom: 28px;
        }
        .hero-btn-primary {
          display: inline-flex;
          align-items: center;
          gap: 8px;
          background: #dc2626;
          color: #fff;
          padding: 13px 26px;
          border-radius: 0;
          font-weight: 700;
          font-size: 14px;
          text-decoration: none;
          box-shadow: 0 8px 24px rgba(220,38,38,0.4);
          transition: background 0.2s, transform 0.2s, box-shadow 0.2s;
        }
        .hero-btn-primary:hover {
          background: #b91c1c;
          transform: translateY(-2px);
          box-shadow: 0 12px 32px rgba(220,38,38,0.5);
        }
        .hero-btn-outline {
          display: inline-flex;
          align-items: center;
          gap: 8px;
          background: rgba(255,255,255,0.12);
          color: #fff;
          padding: 13px 22px;
          border-radius: 0;
          font-weight: 700;
          font-size: 14px;
          text-decoration: none;
          border: 1px solid rgba(255,255,255,0.3);
          backdrop-filter: blur(8px);
          transition: background 0.2s, transform 0.2s;
        }
        .hero-btn-outline:hover {
          background: rgba(255,255,255,0.22);
          transform: translateY(-2px);
        }

        /* Trust strip */
        .hero-trust {
          display: flex;
          align-items: center;
          gap: 14px;
          flex-wrap: wrap;
        }
        .hero-trust-item {
          display: flex;
          align-items: center;
          gap: 6px;
          color: rgba(255,255,255,0.80);
          font-size: 12px;
          font-weight: 600;
        }
        .hero-trust-icon {
          color: #f87171;
        }
        .hero-trust-divider {
          width: 1px;
          height: 16px;
          background: rgba(255,255,255,0.25);
        }

        /* Stats strip (right side, absolute) */
        .hero-stats {
          position: absolute;
          right: 40px;
          bottom: 56px;
          z-index: 2;
          display: flex;
          flex-direction: column;
          gap: 10px;
        }
        .hero-stat-card {
          background: rgba(0,0,0,0.38);
          border: 1px solid rgba(255,255,255,0.14);
          backdrop-filter: blur(16px);
          border-radius: 0;
          padding: 14px 20px;
          text-align: center;
          min-width: 100px;
          transition: transform 0.2s, background 0.2s;
        }
        .hero-stat-card:hover {
          transform: translateY(-3px);
          background: rgba(0,0,0,0.52);
        }
        .hero-stat-num {
          font-size: 22px;
          font-weight: 900;
          color: #f87171;
          line-height: 1;
          margin-bottom: 2px;
          letter-spacing: -0.03em;
        }
        .hero-stat-lbl {
          font-size: 9px;
          color: rgba(255,255,255,0.55);
          font-weight: 600;
          letter-spacing: 0.1em;
          text-transform: uppercase;
        }

        /* Responsive */
        @media (max-width: 768px) {
          .hero-stats { display: none; }
          .hero-content { padding: 0 20px 48px; max-width: 100%; }
        }
        @media (max-width: 480px) {
          .hero-btn-primary, .hero-btn-outline { width: 100%; justify-content: center; }
        }
      `}</style>
    </section>
  );
};

export default Hero;
